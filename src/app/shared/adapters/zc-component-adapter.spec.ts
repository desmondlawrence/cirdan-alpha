import { ZcComponentAdapter } from './zc-component-adapter';

describe('ZcComponentAdapter', () => {
  it('should create an instance', () => {
    expect(new ZcComponentAdapter()).toBeTruthy();
  });
});
