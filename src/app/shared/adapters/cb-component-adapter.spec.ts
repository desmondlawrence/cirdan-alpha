import { CbComponentAdapter } from './cb-component-adapter';

describe('CbComponentAdapter', () => {
  it('should create an instance', () => {
    expect(new CbComponentAdapter()).toBeTruthy();
  });

});
